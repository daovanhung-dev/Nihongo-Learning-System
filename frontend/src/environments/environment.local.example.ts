// Copy to environment.local.ts for local development.
// These are public browser credentials protected by RLS, not server secrets.
export const localEnvironment = {
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabasePublishableKey: 'YOUR_PUBLIC_PUBLISHABLE_KEY',
};
